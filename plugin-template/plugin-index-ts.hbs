import styles from "./styles.less";

const {{ componentName }} = (context: PluginContext) => {
  return {
    dispose: () => {
      /** Remove some side effects */
    },
  };
};

export default {{ componentName }};
